name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      # Install ssh key
      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2.3.1
        with:
          key: '${{ secrets.AC_KEY }}'
          known_hosts: 'just-a-placeholder-so-we-dont-get-errors'

      - name: Generate auth hosts
        run: ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      # Deploy
      - run: rsync --archive --compress --delete . ${{ secrets.FTP_USERNAME }}@${{ secrets.SSH_HOST }}:${{ secrets.SERVER_PATH }}
      - run: rsync -e "ssh -p 20022 -i $HOME/.ssh/known_hosts -o StrictHostKeyChecking=no" --archive --compress --delete . 141.8.192.193/domains/a0900501.xsph.ru/public_html
